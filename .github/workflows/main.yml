name: CI
on: 
  push:
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: | 
        npm init -y
        npm install 
    - name: Run ESLint
      run: npx eslint --ext .ts ./
